You use the spraying technique for this.

Since the range is from 100 - 300. We overwrote everything upto 350 just to be safe. We know that the content start at multiple of 4 so we did that. The return address chosen is buffer_address + 360. Payload starts at 381. 
